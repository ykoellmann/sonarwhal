Todos:
- [x] Fix Tree Display (currently ApioEndpoint(...)) instead of "Methodname" — fixed with EndpointNode wrapper
- [x] Route constraint URL params ({id:int}) now correctly replace path segment, not appended as query
- [x] Hide Params tab when no path/query params; hide Body tab when no body param
- [x] Reference code location — "→ source" link in detail header + right-click "Go to Source" in tree
- [x] Save/load request (headers + body) per endpoint via PropertiesComponent
- [ ] Diff view (compare saved vs current response)
- Reference from code to HttpRequest
- Make response be as json format and highlighting (json viewer or so from jetbrains)
- Make response possible to open in new file to view further
- Make Routes and structure be saved as json, search how to implement this correctly online. Best use something similar to much used formats publicly known to make import / export possible
- Combine saving with persistant saved routes, ... so only changed files get reanalyzed, best check based on change date of file and saved cache or so
- Also save setted params, headers, responses, ... if tab is closed
- For all these points find a smart and clean way to do so, so its possible to extend later (split view between projects, not only controllers, ...)
- [ ] routex-planning.md — new feature planning doc
- Response window gets less high every time i select an endpoint and tree view gets less wide every time
- Later:workflow abhängigkeiten
- openapi integration
- auth
- lazy loading of routes, 